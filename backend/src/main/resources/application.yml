server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/movie_site_app}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:12345}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false

app:
  jwt:
    secret: ${JWT_SECRET:secret_secret_secret_secret_secret_secret_secret_secret_secret_secret}
    expiration-minutes: 1440
  razorpay:
    key-id: ${RAZORPAY_KEY_ID:rzp_test_SuHIafzOuPmCaS}
    key-secret: ${RAZORPAY_KEY_SECRET:IH6afguTn4UzJne5HUYhlSbk}
